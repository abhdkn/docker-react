sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t abhdkn/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true abhdkn/docker-react npm run test -- --coverage